# 基於調色盤的影像色彩遷移方法 — 技術架構詳解

> 論文來源：Chenlei Lv, Dan Zhang，*Palette-based Color Transfer between Images*，arXiv:2405.08263v1，2024年5月。

---

## 一、研究背景與動機

色彩遷移（Color Transfer）是影像增強的重要子課題，其目標是在保留來源影像語義內容的前提下，將參考影像的色彩風格套用至來源影像。現有方法可分為三大類：

1. **全域色彩遷移**：基於全域色彩統計分佈建立映射（如 Reinhard 2001），實作簡單但無法針對語義物件進行差異化處理。
2. **對應關係式色彩遷移**：透過 PatchMatch、SIFT 或 GMM 分群建立像素對應，精度較高但對影像間語義相似度要求嚴格。
3. **深度特徵式色彩遷移**：利用 CNN 或 GAN 萃取語義特徵，效果出色但需大量訓練資料，且對無語義對應的影像仍力有未逮。

本論文提出一套**全自動調色盤色彩遷移框架**，不依賴複雜語義分析，亦不需使用者手動設定，透過三個核心元件（調色盤分群、色彩映射策略、光照優化）解決上述問題。

---

## 二、整體方法架構

方法流程如下圖所示（共四個主要階段）：

```
輸入影像（來源 + 參考）
        │
        ▼
┌──────────────────────────┐
│  1. 調色盤分群             │  ← 建立來源與參考影像各自的調色盤
│  (Palette-based Clustering)│
└──────────────────────────┘
        │
        ▼
┌──────────────────────────┐
│  2. 前景/背景分離           │  ← 使用 DeepLabV3+ 語意分割
│  (Foreground/Background)  │
└──────────────────────────┘
        │
        ▼
┌──────────────────────────┐
│  3. 色彩映射策略            │  ← 分割對應 + 色差控制 + 色彩一致性維持
│  (Color Mapping Strategy) │
└──────────────────────────┘
        │
        ▼
┌──────────────────────────┐
│  4. 光照優化（選用）         │  ← 加權更新 L 通道 + 全域光照增強
│  (Lighting Optimization)  │
└──────────────────────────┘
        │
        ▼
    輸出影像（色彩遷移結果）
```

---

## 三、第一元件：調色盤分群（Palette-based Clustering）

### 3.1 設計動機

傳統調色盤分群（如 k-means）需手動指定分群中心數，不同的中心數對同一影像會產生不同調色盤，導致遷移效果不可預測。本論文提出以**色彩直方圖峰值搜尋**為核心的自適應分群法，無需使用者指定。

### 3.2 Lab 色彩空間的選擇

方法在 **Lab 色彩空間**中進行分析，原因如下：
- RGB 空間的三通道資訊耦合，難以分離色彩與亮度。
- Lab 空間中亮度（L）與色彩（a、b）解耦，能有效消除不同光照條件的干擾，使峰值搜尋更加精準。

### 3.3 實作細節

調色盤分群包含三個核心步驟：

#### 步驟一：直方圖建構（Histogram Construction）

將 Lab 各通道 $(l_i, a_i, b_i)$ 分別劃分為 $z$ 個分箱（預設 $z = 100$），將像素累積至對應分箱，形成離散的色彩分佈表示：

$$I_H = \{b_0, \ldots, b_z\},\quad b_i = (l_i, a_i, b_i),\quad i \in [0, z]$$

#### 步驟二：峰值搜尋（Peak Searching）

逐一比較各分箱，選取在局部範圍 $N\{b_i\}$ 內具有最大值的分箱作為候選峰值集合 $\{b_p\}$：

$$\{b_p\} = \max\{b_i \mid N(b_i),\ b_i > b_{\min}\},\quad N\{b_i\} = \{b_{i-r}, \ldots, b_{i+r}\}$$

- 搜尋半徑 $r$ 預設為 3，定義局部範圍大小。
- 設置最小閾值 $b_{\min} = 30$，過濾統計意義不足的假峰值，避免干擾調色盤準確性。

#### 步驟三：峰值合併（Peak Merging）

從 Lab 三通道各自取得候選峰值後合併為一個集合，建構 **kd-tree**。將每個像素的 Lab 值輸入 kd-tree，統計各峰值所對應的像素數量，並設定峰值數量上限 $t$（預設 $t = 32$）：

$$\{b_{fp}\} = \{b_i \mid K(b_i) > K(b_t),\ b_i \in \{b_p\}\}$$

最終，所有像素依距離最近原則分類至最終峰值集合 $\{b_{fp}\}$：

$$p_i = \arg\min\ d(L(p_i),\ b_i),\quad b_i \in \{b_{fp}\}$$

其中 $L(p_i)$ 為像素 $p_i$ 的 Lab 值，$d$ 為 Lab 空間中的歐氏距離。各分類的平均色彩值即為調色盤項目。

> **設計要點**：上限數 $t = 32$ 基於實驗經驗設定，能兼顧色彩資訊保留的完整性與調色盤的聚合特性。

---

## 四、第二元件：色彩映射策略（Color Mapping Strategy）

調色盤建立後，色彩遷移任務轉化為尋找映射函數 $f: I_s\{b_{fp}\} \rightarrow I_r\{b_{fp}\}$，其中 $I_s$ 為來源影像，$I_r$ 為參考影像。

本論文提出三項約束條件共同構成映射策略：

### 4.1 分割對應（Split Correspondence）

#### 原理

自然影像通常可分為**前景**（人物、動物、建築等語義物件）與**背景**（環境脈絡）。對兩者分別進行色彩映射，既保留語義一致性，又不需嚴格的跨影像語義對應關係。

#### 實作

採用 **DeepLabV3+**（帶有空洞可分離卷積的編碼器-解碼器架構）執行語義分割，分離前景與背景。映射函數因此分為兩個獨立部分：

$$f = \begin{cases} f_{fore}: I_s\{b_{fp}\}_{fore} \rightarrow I_r\{b_{fp}\}_{fore} \\ f_{back}: I_s\{b_{fp}\}_{back} \rightarrow I_r\{b_{fp}\}_{back} \end{cases}$$

> **泛化性設計**：若影像無法明確分離前景與背景，所有像素視為前景，確保方法對各類影像皆有適用性。

### 4.2 色差控制（Chromatic Aberration Control）

#### 原理

隨機色彩映射可能將高亮度區域的色彩映射至低亮度色彩，破壞光照語義表示。為此，採用**最近鄰搜尋**策略控制映射合理性：

$$f_i: b_i \rightarrow b_j,\quad b_j = \arg\min\ d(b_i,\ b_j)$$

在 Lab 空間以最近距離原則，讓來源影像的每個調色盤峰值映射至參考影像最接近的峰值。此設計雖降低映射靈活性，但有效避免色彩混亂，提升實際應用中的泛化能力。

### 4.3 色彩一致性維持（Color Consistency Keeping）

最近鄰搜尋可能導致**多對一映射**（來源影像多個峰值映射至同一參考峰值），破壞色彩分佈結構。為此設計兩項子機制：

#### 4.3.1 內部一致性維持（Internal Consistency Keeping）

當多個來源峰值 $\{b_I\}$ 映射至同一參考峰值 $b_j$ 時，選取像素數量最多的峰值 $b_k$ 維持映射，其餘峰值加入待處理集合 $\{b_Q\}$：

$$f_k: b_k \rightarrow b_j,\quad b_k = \max\{b_i \mid b_i \in \{b_I\}\}$$

此機制消除多對一映射，避免色彩分佈結構損毀。

#### 4.3.2 外部連續性維持（External Continuity Keeping）

對待處理集合 $\{b_Q\}$ 中的峰值，依已有映射結果加權計算其映射值：

$$f_q: b_q \rightarrow \sum_{b_k \in N(b_q)} w_k f(b_k),\quad b_q \in \{b_Q\}$$

$$w_k = \frac{d(b_k, b_q)^{-1}}{\sum_{b_k \in N(b_q)} d(b_k, b_q)^{-1}}$$

權重 $w_k$ 以距離倒數計算，距離越近的已映射峰值影響越大，保障色彩分佈的外部連續性。

### 4.4 最終像素色彩計算

整合上述三項約束後，來源影像每個像素 $p_i$ 的色彩遷移結果為：

$$f(p_i) = f(b_i) + L(p_i) - b_i$$

其中 $b_i$ 為像素所屬的調色盤峰值，$L(p_i)$ 為像素的 Lab 值。此公式在映射調色盤的同時保留了像素與調色盤中心之間的相對偏差，維持局部細節。

> **注意**：此階段僅更新 $a$ 通道與 $b$ 通道，L 通道（光照強度）由光照優化步驟單獨處理。

---

## 五、第三元件：光照優化（Lighting Optimization）

### 5.1 設計動機

色彩映射階段僅更新 $a$、$b$ 通道（色彩資訊），L 通道（光照強度）需獨立處理，原因如下：
1. 直接套用公式 (11) 更新 L 通道，將把參考影像的異常曝光轉移至結果。
2. 調色盤映射為離散計算，存在產生不連續色彩分佈的風險。

### 5.2 色彩映射加權更新（Color Mapping-based Weighted Update）

以加權方式融合原始 L 通道與映射結果中的 L 值：

$$L(p'_i)_l = (1 - \alpha) L(p_i)_l + \alpha f(p_i)_l$$

- $\alpha$ 為映射結果的權重，預設設定為 **0.3**，在引入參考影像光照特性的同時，充分保留來源影像的原始光照。

### 5.3 全域光照增強（Global Lighting Enhancement，選用）

當參考影像存在明顯異常曝光時，可選用全域光照增強步驟：
- 基於 Ma et al. (CVPR 2022) 提出的低光影像增強框架對 L 通道進行優化。
- 提取增強後的 L 通道值重新指派為 $L(p')_l$，有效抑制因參考影像曝光異常導致的遷移品質下降。

---

## 六、方法貢獻總結

| 元件 | 解決的問題 | 核心技術 |
|------|-----------|---------|
| 調色盤分群 | 傳統分群需手動設定中心數，對散亂色彩分佈適用性差 | Lab 直方圖峰值搜尋 + kd-tree 合併 |
| 分割對應 | 跨影像嚴格語義對應要求限制適用範圍 | DeepLabV3+ 前景/背景分離 |
| 色差控制 | 隨機映射導致色彩混亂 | Lab 空間最近鄰搜尋 |
| 色彩一致性 | 多對一映射破壞色彩分佈結構 | 內部一致性 + 外部連續性維持 |
| 光照優化 | 參考影像異常曝光轉移 | 加權 L 通道更新 + 全域光照增強 |

---

## 七、實驗設置

- **測試資料集**：VOC2012（792 張可分離影像）、Zero-DCE 資料集（8 張參考影像，1496 張來源影像）、網路隨機選圖。
- **硬體平台**：Intel Xeon W 2133 3.6GHz（CPU）、32 GB RAM、Quadro P620（GPU）。
- **開發環境**：Visual Studio 2019（64 位元），Windows 11。

### 評估指標

1. **色彩一致性分析（Color Consistency Analysis）**：計算來源影像與遷移結果在 RGB（10 bins）與 L（20 bins）通道各分箱內的變異數平均值，量化色彩偏差程度。
2. **衰退率計算（Fading Rate Computation）**：比較來源影像與遷移結果在 $a$、$b$ 通道的信號強度損失，評估色彩是否因遷移而褪色。
3. **無參考影像品質評估（NR-IQA）**：採用 Brisque、ClipIQA+、CNNIQA、DBCNN、MUSIQ、NIMA 六項評估工具進行全域品質量化。

### 主要比較方法

- Reinhard et al.（Lab 全域色彩遷移）
- Tai et al.（GMM 分群色彩遷移）
- He et al.（深度典範式色彩遷移，CNN-based）
- Lee et al.（深度直方圖分析色彩遷移）

---

## 八、方法限制

1. **色彩分佈極端差異**：若來源與參考影像的色彩分佈範圍差異過大或過度集中，方法可能退化為類似濾鏡的全域色彩對應，損失局部精準性。
2. **計算效率**：調色盤分群與色彩映射的時間複雜度與影像尺寸成正比，300×300 解析度的影像平均需 1～3 分鐘，目前不支援即時（real-time）處理，無法直接應用於影片處理。

---

*文件生成日期：2026年5月5日*
